##PROBLEM STATEMENT
#Masalah yang ingin diselesaikan yaitu apakah mayoritas pembeli menggunakan voucher diskon, data yang dimiliki yaitu food_delivery_datasets.csv, sehingga algoritmanya yaitu :
#1. Import data dan library
#2. Pilih kolom voucher_amount
#3. cleaning data dengan status completed
#4. Jika value voucher_amount>0, maka pembeli menggunakan voucher (yes), jika value voucher_amount=0, maka pembeli tidak menggunakan voucher (no)
import pandas as pd
from collections import Counter
PATH_DATA = '../mydata/'
df = pd.read_csv(PATH_DATA+'food_delivery_datasets.csv')
group_status = df.groupby('status')
group_status['status'].size()
voucher_amount2=[]
if 'voucher_amount'==0:
  voucher_amount2+='yes'
  else:
    voucher_amount2+='no'
    
c=counter(voucher_amount2)
c.most_common()
value, count=c.most_common()[0]
print value

##KESIMPULAN DAN SARAN
#Dari sini dapat disimpulkan bahwa dari 881 order yang berstatus completed, mayoritas pembeli TIDAK menggunakan voucher diskon
